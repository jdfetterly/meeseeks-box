# Documentation Archive

This folder preserves documents that are useful historical context but are no longer active implementation, QA, or design guidance.

Current operating guidance should live in `docs/operations/` and current planning guidance should live in `planning/`.

## Archived On 2026-05-02

- `ui-screen-audit/`: March 2026 desktop/responsive shell screenshot audit. It remains useful as historical evidence for the older desktop shell, but it is not current mobile guidance. Current mobile lives at `/mobile` in `app/mobile/page.tsx` and `components/mobile/*`.
