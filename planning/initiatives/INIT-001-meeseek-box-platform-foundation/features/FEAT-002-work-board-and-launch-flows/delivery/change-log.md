# Change Log

- Artifact: Delivery Change Log
- Project: `PROJ-001 Meeseek Box`
- Initiative ID: `INIT-001`
- Feature ID: `FEAT-002`
- Status: `active`
- Last Updated: `2026-03-22`

## Entries

- `2026-03-18`: Created initial full TDD for launch flows, work board behavior, and chat escalation; added delivery task scaffolds.
- `2026-03-19`: Replaced high-level task list with dependency-ordered implementation task plan and verification matrix.
- `2026-03-19`: Added explicit one-shot discovery, source labeling, and minimum schedule-health scope.
- `2026-03-21`: Implemented the canonical preset and schedule persistence layer with product-state APIs, plus the first backend launch contract. `run now`, canonical `schedule once`, and separate saved drafts now flow through shared product-state services; drafts live outside the main board and can be promoted into real work later.
- `2026-03-21`: Added a separate draft registry with list/delete/promote APIs and surfaced drafts inside the Work experience as saved launch intent rather than pseudo-board items. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Added canonical board-lane querying and a starter Work surface that consumes server-backed board summaries instead of browser-local state. Wired a minimal launch composer into the Work page so operators can create canonical work items and scheduled intent directly from the app.
- `2026-03-21`: Added the first canonical chat surface with shared conversations/messages and a starter conversation-to-work creation flow, so chat can now produce linked work items in the server-backed product-state layer.
- `2026-03-21`: Wired canonical `schedule once` launches to runtime-native OpenClaw one-shot cron creation. The launch flow now supports local execution on the mini or a narrow SSH bridge in development, persists `external_job_id` on success, and records `sync_failed` schedules explicitly when runtime sync fails so operators do not lose visibility of partial-success launches. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Added runtime-native schedule reconciliation using OpenClaw cron list/run history, plus a starter Schedules refresh action. Canonical schedule summaries can now move from `scheduled` to `completed`, `failed`, or `missed` based on runtime evidence without inventing canonical runs prematurely. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Added canonical work-item and run detail pages and linked existing board, Inbox, and artifact surfaces into them. The server-backed product-state UI can now drill from summaries into related work/run context instead of stopping at list views. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Implemented separate launch drafts with list/delete/promote APIs, surfaced drafts in Work outside the main board, and added promotion into either `run now` or `schedule once`. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Added a conservative attach-to-existing conversation flow so chat escalation is no longer create-only. Existing work can now be linked from a conversation when it is unlinked or already attached to the same thread. Revalidated with focused `vitest` coverage and `npx next build --webpack`.
- `2026-03-21`: Completed the first real browser validation pass for canonical launch, draft promotion, chat messaging, and attach-to-existing using Playwright against the local dev server. The pass exposed two broader shell-level issues: shared navigation/search were still calling legacy `/api/crons`, and attached conversations were not visible on work surfaces after a successful attach. Replaced those legacy fetches with canonical schedule reads, added visible conversation linkage on Work cards and work detail, and revalidated in-browser with zero console errors on the canonical pages.
- `2026-03-21`: Added schedule detail pages and linked canonical schedule summaries into them from both the Schedules list and attention surfaces. Operators can now inspect runtime sync state, canonical health, linked work, and future schedule-produced artifacts from a dedicated server-backed drilldown. Revalidated with `npx next build --webpack` and live browser validation against the local dev server.
- `2026-03-22`: Extended schedule detail into the first real output-management surface by adding in-place registration of scheduled-job workspace files as artifact families. This keeps v1 light while giving scheduled work a concrete place to surface durable outputs. Revalidated with focused `vitest` coverage, `npx next build --webpack`, and live browser validation against the local dev server.
- `2026-03-22`: Replaced manual schedule-output registration as the intended v1 flow with an explicit producer-report path. Schedule detail still provides a browser tool for local validation, but the canonical contract is now “scheduled job reports file path on completion,” which also updates schedule health and resolves stale missed-schedule attention alongside artifact versioning.
- `2026-03-22`: Added two installable starter jobs to the Work surface: `Morning Ops Brief` and `Weekly System Review`. Both are self-contained presets rather than hidden agent behavior, and both include read-only childcare-sheet instructions (`blue = JD`, `green = Kayla`, never modify the sheet) in their prompt templates. Installed presets now appear directly in the launch composer so they can be applied into the form instead of sitting passively in storage.
- `2026-03-22`: Extended starter jobs from “installable presets” into light recurring operating loops. Added a narrow recurring-schedule path for installed starter jobs with suggested daily/weekly cadences, runtime-native `cron add --cron ... --tz ...` sync, canonical work-item/schedule creation, and prompt hydration so the explicit `report-schedule-output` contract carries the real schedule id. Revalidated with focused `vitest` coverage, `npx next build --webpack`, and live browser validation that scheduled both `Morning Ops Brief` and `Weekly System Review` from the Work page into canonical recurring schedules.
- `2026-03-22`: Added recurring schedule lifecycle actions for starter-job schedules: edit cadence, pause, resume, and delete. Deleting a recurring schedule now archives the linked standing work item so it drops off the main Work board instead of lingering as pseudo-active work. Also tightened the shell-level read path so deleted schedules are hidden from the main Schedules list and nav badge counts, and runtime-sync copy no longer leaks stale internal markers for lifecycle-mutated schedules. Revalidated with focused `vitest` coverage, `npx next build --webpack`, and live browser validation against the recurring schedule detail, linked archived work detail, and shared Schedules navigation.
- `2026-03-22`: Pulled linked schedule ownership and recurring controls into work detail so schedule-owned work can be inspected and managed from the primary work object, not only from `/schedules`. Revalidated with `npx next build --webpack` and live iPhone-width browser validation against a recurring starter-job work item.
